import { IconButton, styled } from "@material-ui/core"
import React from "react"

const IcoCont = styled(IconButton)({
    padding: "10px",
    borderRadius: "10px",
    boxShadow: "0px 0px 6px 0px rgba(0,0,0,0.75)",
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
});
export default IcoCont;

